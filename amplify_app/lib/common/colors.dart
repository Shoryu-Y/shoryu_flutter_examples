import 'package:flutter/material.dart';

class AppColors {
  const AppColors();

  static const brandColor = Colors.lightBlueAccent;

  static const waitingColor = Colors.amber;
  static const inProgressColor = Colors.green;
  static const doneColor = Color(0xFFe4007f);
  static const archiveColor = Colors.blueGrey;
}
